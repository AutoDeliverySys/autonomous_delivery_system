// ignore_for_file: file_names

const Duration databaseDelay = Duration(milliseconds: 2000);
const Duration constantDelay = Duration(milliseconds: 3000);

const Duration smallDelay = Duration(milliseconds: 1000);
const Duration averageDelay = Duration(milliseconds: 2000);
const Duration longDelay = Duration(milliseconds: 5000);

const double locationErrorRatio = 0.01;

const String routePageWrapper = "/";
const String routePageAuthenticate = "/authenticate";
const String routePageHome = "/home";
const String routePageMap = "/map";
const String routePageProfile = "/profile";
const String routePageOrders = "/orders";
const String routePageLoading = "/loading";
